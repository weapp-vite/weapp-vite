import type { WeapiCrossPlatformAdapter } from '../../types'

export interface WeapiCrossPlatformMethodDocsMissingMethodsStartGyroscopeToWriteBLECharacteristicValue {
  /**
   * 对应微信小程序 `wx.startGyroscope` 的 API。
   *
   * 分类：设备
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/device/gyroscope/wx.startGyroscope.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('startGyroscope')` 与 `wpi.supports('startGyroscope')` 判断。
   */
  startGyroscope: WeapiCrossPlatformAdapter['startGyroscope']

  /**
   * 对应微信小程序 `wx.startPullDownRefresh` 的 API。
   *
   * 分类：界面
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/ui/pull-down-refresh/wx.startPullDownRefresh.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('startPullDownRefresh')` 与 `wpi.supports('startPullDownRefresh')` 判断。
   */
  startPullDownRefresh: WeapiCrossPlatformAdapter['startPullDownRefresh']

  /**
   * 对应微信小程序 `wx.startWifi` 的 API。
   *
   * 分类：设备
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/device/wifi/wx.startWifi.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('startWifi')` 与 `wpi.supports('startWifi')` 判断。
   */
  startWifi: WeapiCrossPlatformAdapter['startWifi']

  /**
   * 对应微信小程序 `wx.stopAccelerometer` 的 API。
   *
   * 分类：设备
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/device/accelerometer/wx.stopAccelerometer.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('stopAccelerometer')` 与 `wpi.supports('stopAccelerometer')` 判断。
   */
  stopAccelerometer: WeapiCrossPlatformAdapter['stopAccelerometer']

  /**
   * 对应微信小程序 `wx.stopBeaconDiscovery` 的 API。
   *
   * 分类：设备
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/device/ibeacon/wx.stopBeaconDiscovery.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('stopBeaconDiscovery')` 与 `wpi.supports('stopBeaconDiscovery')` 判断。
   */
  stopBeaconDiscovery: WeapiCrossPlatformAdapter['stopBeaconDiscovery']

  /**
   * 对应微信小程序 `wx.stopBluetoothDevicesDiscovery` 的 API。
   *
   * 分类：设备
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/device/bluetooth/wx.stopBluetoothDevicesDiscovery.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('stopBluetoothDevicesDiscovery')` 与 `wpi.supports('stopBluetoothDevicesDiscovery')` 判断。
   */
  stopBluetoothDevicesDiscovery: WeapiCrossPlatformAdapter['stopBluetoothDevicesDiscovery']

  /**
   * 对应微信小程序 `wx.stopCompass` 的 API。
   *
   * 分类：设备
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/device/compass/wx.stopCompass.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('stopCompass')` 与 `wpi.supports('stopCompass')` 判断。
   */
  stopCompass: WeapiCrossPlatformAdapter['stopCompass']

  /**
   * 对应微信小程序 `wx.stopGyroscope` 的 API。
   *
   * 分类：设备
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/device/gyroscope/wx.stopGyroscope.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('stopGyroscope')` 与 `wpi.supports('stopGyroscope')` 判断。
   */
  stopGyroscope: WeapiCrossPlatformAdapter['stopGyroscope']

  /**
   * 对应微信小程序 `wx.stopPullDownRefresh` 的 API。
   *
   * 分类：界面
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/ui/pull-down-refresh/wx.stopPullDownRefresh.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('stopPullDownRefresh')` 与 `wpi.supports('stopPullDownRefresh')` 判断。
   */
  stopPullDownRefresh: WeapiCrossPlatformAdapter['stopPullDownRefresh']

  /**
   * 对应微信小程序 `wx.stopWifi` 的 API。
   *
   * 分类：设备
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/device/wifi/wx.stopWifi.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('stopWifi')` 与 `wpi.supports('stopWifi')` 判断。
   */
  stopWifi: WeapiCrossPlatformAdapter['stopWifi']

  /**
   * 跳转到 tabBar 页面，并关闭其他所有非 tabBar 页面。
   *
   * 对应微信文档：`wx.switchTab(Object object)`
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/route/wx.switchTab.html
   *
   * 说明：
   * - 路径必须是 tabBar 页面路径。
   * - 跳转后会关闭其他非 tabBar 页面。
   * - 不适合依赖 query 传递复杂参数的场景。
   *
   * 示例：
   * ```ts
   * await wpi.switchTab({
   *   url: '/pages/home/index',
   * })
   * ```
   */
  switchTab: WeapiCrossPlatformAdapter['switchTab']

  /**
   * 对应微信小程序 `wx.uploadFile` 的 API。
   *
   * 分类：网络
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/network/upload/UploadTask.html
   *
   * 说明：
   * - 常用于头像、图片、附件、日志文件等本地资源上传。
   * - 一般会与 `chooseImage`、`chooseMessageFile`、`downloadFile` 等 API 组合使用。
   * - 服务端若要求额外表单字段，可通过 `formData` 一并提交。
   *
   * 示例：
   * ```ts
   * const result = await wpi.uploadFile({
   *   url: 'https://example.com/api/upload',
   *   filePath: tempFilePath,
   *   name: 'file',
   *   formData: {
   *     bizType: 'avatar',
   *   },
   * })
   *
   * console.log(result.statusCode, result.data)
   * ```
   *
   * 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('uploadFile')` 与 `wpi.supports('uploadFile')` 判断。
   */
  uploadFile: WeapiCrossPlatformAdapter['uploadFile']

  /**
   * 对应微信小程序 `wx.vibrateLong` 的 API。
   *
   * 分类：设备
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/device/vibrate/wx.vibrateLong.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('vibrateLong')` 与 `wpi.supports('vibrateLong')` 判断。
   */
  vibrateLong: WeapiCrossPlatformAdapter['vibrateLong']

  /**
   * 对应微信小程序 `wx.vibrateShort` 的 API。
   *
   * 分类：设备
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/device/vibrate/wx.vibrateShort.html
   *
   * 说明：
   * - 用于触发短振动反馈，适合按钮确认、轻量提醒、游戏触感等场景。
   *
   * 示例：
   * ```ts
   * await wpi.vibrateShort()
   * ```
   *
   * 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('vibrateShort')` 与 `wpi.supports('vibrateShort')` 判断。
   */
  vibrateShort: WeapiCrossPlatformAdapter['vibrateShort']

  /**
   * 对应微信小程序 `wx.writeBLECharacteristicValue` 的 API。
   *
   * 分类：设备
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/device/bluetooth-ble/wx.writeBLECharacteristicValue.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('writeBLECharacteristicValue')` 与 `wpi.supports('writeBLECharacteristicValue')` 判断。
   */
  writeBLECharacteristicValue: WeapiCrossPlatformAdapter['writeBLECharacteristicValue']

}
