#
# Web 运行时的可选环境变量（复制为 .env.local/.env.development 使用）
#

# 修改浏览器端 Dev Server 监听地址（默认 127.0.0.1）
WEAPP_WEB_HOST=127.0.0.1

# 修改 Dev Server 端口（默认 5173）
WEAPP_WEB_PORT=5173

# 启动时自动在浏览器中打开（默认 false）
WEAPP_WEB_OPEN=false
