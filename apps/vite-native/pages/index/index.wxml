<view class="min-h-screen bg-gradient-to-b from-indigo-50 via-slate-50 to-emerald-50 text-slate-900 px-6 py-6 space-y-6">
  <view class="flex items-start justify-between">
    <view class="space-y-2">
      <view class="text-3xl font-bold tracking-tight">weapp-vite Showcase</view>
      <view class="text-sm text-slate-600">覆盖运行时、构建、分包、样式的全部能力示例，全部采用 Tailwind 原子化样式。</view>
    </view>
    <view class="px-3 py-2 rounded-full bg-white shadow text-xs font-semibold text-slate-700">v{{version}}</view>
  </view>

  <view class="bg-white rounded-3xl p-5 shadow-lg space-y-3">
    <view class="flex items-center justify-between">
      <view class="text-base font-semibold">环境</view>
      <view class="flex items-center gap-2">
        <button
          class="px-3 py-2 rounded-lg bg-slate-900 text-white text-xs active:bg-slate-800"
          open-type="share"
        >转发给朋友</button>
        <button class="px-3 py-2 rounded-lg bg-slate-900 text-white text-xs active:bg-slate-800" @tap="handleCopyEnv">复制</button>
      </view>
    </view>
    <scroll-view scroll-y class="max-h-44 bg-slate-100 rounded-xl p-3 text-xs font-mono text-slate-700 break-all">
      {{envPreview}}
    </scroll-view>
  </view>

  <view class="grid grid-cols-1 gap-4">
    <view
      wx:for="{{sections}}"
      wx:key="url"
      class="bg-white rounded-3xl p-5 shadow-lg active:scale-[0.99] duration-150"
      data-url="{{item.url}}"
      @tap="handleNavigate"
    >
      <view class="flex items-center justify-between">
        <view class="text-xl font-semibold">{{item.title}}</view>
        <view class="text-xs px-3 py-1 rounded-full bg-indigo-100 text-indigo-700">查看</view>
      </view>
      <view class="text-sm text-slate-600 mt-2">{{item.desc}}</view>
    </view>
  </view>

  <view class="text-xs text-slate-500">最后更新：{{lastUpdated}}</view>
</view>
