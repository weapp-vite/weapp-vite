<view class="min-h-screen bg-slate-900 text-slate-50 px-6 py-5 space-y-6">
  <view class="space-y-2">
    <view class="text-3xl font-semibold">编译 & 能力总览</view>
    <view class="text-sm text-slate-300">对应官网文档的核心特性，链接到示例页面而非运行时魔法。</view>
  </view>

  <view class="bg-slate-800 rounded-2xl p-4 space-y-3">
    <view class="flex items-center justify-between">
      <view class="text-sm text-slate-300">环境变量 (import.meta.env)</view>
      <button class="px-3 py-2 rounded-lg bg-slate-50 text-slate-900 text-xs active:bg-slate-200" @tap="handleCopyEnv">复制</button>
    </view>
    <scroll-view scroll-y class="max-h-56 bg-slate-900/60 rounded-xl p-3 text-xs font-mono break-all">
      {{envPreview}}
    </scroll-view>
  </view>

  <view class="bg-slate-800 rounded-2xl p-4 space-y-3">
    <view class="flex items-center justify-between">
      <view class="text-base font-medium">语言 / 语法</view>
      <view class="text-xs text-slate-400">对应文档：语言支持</view>
    </view>
    <view class="flex flex-wrap gap-2">
      <view class="px-3 py-2 rounded-xl bg-indigo-500/20 text-indigo-100 text-xs" wx:for="{{languageFeatures}}" wx:key="*this">{{item}}</view>
    </view>
  </view>

  <view class="bg-slate-800 rounded-2xl p-4 space-y-3">
    <view class="flex items-center justify-between">
      <view class="text-base font-medium">构建 / 分包</view>
      <button class="px-3 py-2 rounded-lg bg-amber-400 text-slate-900 text-xs font-semibold active:bg-amber-500" @tap="handleNavigateSubpackages">查看分包示例</button>
    </view>
    <view class="flex flex-wrap gap-2">
      <view class="px-3 py-2 rounded-xl bg-amber-500/20 text-amber-100 text-xs" wx:for="{{buildFeatures}}" wx:key="*this">{{item}}</view>
    </view>
  </view>

  <view class="bg-slate-800 rounded-2xl p-4 space-y-3">
    <view class="flex items-center justify-between">
      <view class="text-base font-medium">样式 / 组件</view>
      <button class="px-3 py-2 rounded-lg bg-emerald-400 text-slate-900 text-xs font-semibold active:bg-emerald-500" @tap="handleNavigateUI">跳转 UI 示例</button>
    </view>
    <view class="flex flex-wrap gap-2">
      <view class="px-3 py-2 rounded-xl bg-emerald-500/20 text-emerald-100 text-xs" wx:for="{{styleFeatures}}" wx:key="*this">{{item}}</view>
    </view>
  </view>

  <view class="bg-slate-800 rounded-2xl p-4 space-y-3">
    <view class="flex items-center justify-between">
      <view class="text-base font-medium">调试 / 诊断</view>
      <view class="text-xs text-slate-400">对应文档：调试 & 性能</view>
    </view>
    <view class="flex flex-wrap gap-2">
      <view class="px-3 py-2 rounded-xl bg-slate-500/20 text-slate-100 text-xs" wx:for="{{runtimeHooks}}" wx:key="*this">{{item}}</view>
    </view>
  </view>
</view>
