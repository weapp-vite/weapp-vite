---
"weapp-vite": patch
"create-weapp-vite": patch
---

修复 classic 开发模式下共享模板和嵌套 import/include 更新后仍输出旧内容的问题。增量构建会刷新整个变更批次的模板源与依赖图，隔离构建期间到达的后续事件，并在下一次请求时重放失败批次。删除后恢复模板时不再沿用旧扫描结果或已清空目录的输出缓存，全量重建也会保留未受影响页面的模板；同时正确区分名称前缀相似的分包。
