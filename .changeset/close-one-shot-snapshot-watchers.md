---
"weapp-vite": patch
"create-weapp-vite": patch
---

修复开发模式的一次性 HMR 快照构建遗留监听器、导致组件引用方被重复失效的问题。根据 Vite 的实际监听生命周期与 classic 开发控制器的资源所有权保留常驻服务，在独立快照完成或编译失败后等待监听器回收。classic 控制器等待当前快照结束后统一关闭共享资源，避免快照关闭自身 watcher 产生自等待并中断后续更新。
