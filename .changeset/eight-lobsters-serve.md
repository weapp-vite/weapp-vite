---
"@wevu/api": patch
---

增强 `weapi` 三端语义对齐能力，补充 `chooseMedia`、`chooseMessageFile`、`setBackgroundColor`、`setBackgroundTextStyle`、`getNetworkType`、`getBatteryInfo`、`getBatteryInfoSync` 的跨端映射与参数/结果归一化；同时新增对应单元测试、类型文档矩阵与 API 兼容性报告更新，提升支付宝与抖音在微信命名下的可调用一致性与语义覆盖。
