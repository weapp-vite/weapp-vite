---
"weapp-vite": patch
"create-weapp-vite": patch
---

修复状态保持热更新后，新打开的原生页面和新创建的组件仍使用首次注册默认数据的问题。新实例在生命周期开始前读取更新后的默认值，组件渲染同步延后到 attached，并按组件声明保留父级传入属性和活跃实例的交互状态，避免微信宿主将全部 data 镜像为实例 properties 时漏掉默认值更新。
