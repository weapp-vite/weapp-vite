---
"@mpcore/simulator": patch
---

为浏览器页面渲染提供真实 WXSS 依赖解析，按层叠顺序加载全局、页面及递归本地导入样式，也支持页面内联全局样式快照的连续更新和仅样式更新，保留页面自有样式优先级及页面、App 和交互状态。支持 Component 页面的 `page-*` 选项关闭隐式全局样式，按最终页面 JSON 优先于 JS 定义的规则解析隔离配置，并明确报告缺失、循环及不支持的导入；Web 预览使用解析结果，避免示例皮肤遮盖页面样式。
