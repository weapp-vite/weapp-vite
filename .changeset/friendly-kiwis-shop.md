---
"weapp-vite": minor
"create-weapp-vite": patch
---

在 `weapp-vite` 中集成 `@weapp-vite/mcp`：新增 `weapp-vite mcp` CLI 命令用于直接启动 MCP stdio 服务，新增 `weapp-vite/mcp` 程序化导出入口，并补充详细的 MCP 使用文档（启动方式、客户端接入、工具与资源说明、安全边界与排障）。
