---
"weapp-vite": patch
"create-weapp-vite": patch
---

修复独立分包复用主包组件和页面布局时覆盖主包产物的问题。共享组件、布局及其静态资源现在生成到独立分包内部，并保持主包与独立分包各自的引用关系，避免页面启动时加载错误的分包运行时。

同时修复通过符号链接目录构建时，真实源文件路径与项目路径不一致导致独立分包页面被识别为组件、丢失布局的问题。

修复 stateful HMR 桥的页面与组件实例跟踪，覆盖未显式声明生命周期的原生页面和组件。热更新重载时恢复对应实例的交互状态并清理已卸载实例，普通导航的新实例保持独立初始状态，避免误用同模块旧快照；Wevu 重载时保留必要的内部跟踪与恢复步骤。
