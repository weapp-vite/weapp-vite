---
'@weapp-vite/vscode': minor
'create-weapp-vite': minor
---

VS Code 扩展现在支持在页面 `.vue` 的 `definePageJson({...})` 与 `<json>` 页面配置中补全常用枚举值和布尔值，例如 `navigationStyle`、`backgroundTextStyle`、`enablePullDownRefresh` 与 `disableScroll`，进一步减少页面配置手写成本。
