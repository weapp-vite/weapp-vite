---
"weapp-vite": patch
"create-weapp-vite": patch
---

修复自动导入 Vue 组件的纯脚本更新被父页面依赖摘要误判为完整重载的问题。补齐初始 CJS 模块图，统一使用 Vite 最终 root 生成首包和补丁的模块 ID，并在替换工厂前依据已执行的旧依赖图查找接收边界，使组件更新传播到实际执行注册的逻辑入口，避免只替换配置对象而没有更新已有组件实例；更新前检查工厂完整性，保留热更新数据与旧回调，缺失工厂或接收边界时明确回退。JSX 入口源码由实际源码模块显式监听，避免页面脚本更新额外使 App 注册入口失效。父引用摘要保留模板、样式和 JSON 配置宏的变化，解析失败时保守追踪完整源码。
