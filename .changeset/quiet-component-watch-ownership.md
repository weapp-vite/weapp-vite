---
"weapp-vite": patch
"create-weapp-vite": patch
---

开发时由组件发现侧车独立维护自动导入的目录监听，避免无关临时文件进入原生 HMR 并触发额外重建；同时保留组件目录首次创建、删除恢复及根目录通配符的自动发现。复制资源独立复用构建扫描规则监听文件更新和新增，保留默认资源及 `copy.include/exclude/filter` 的处理，不再依赖自动导入的宽目录监听。

为逻辑入口读取的宿主源码和独立子构建依赖建立精确的原生监听关系，保留 App 配置路径切换及独立模板更新；固定逻辑依赖输出顺序，避免纯脚本更新产生多余包装模块补丁。
