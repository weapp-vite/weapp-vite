---
'@weapp-vite/vscode': minor
'create-weapp-vite': minor
---

VS Code 扩展现在支持在页面标题双写不一致时提供双向 quick fix，不仅可以将 `<json>` 的 `navigationBarTitleText` 同步为 `definePageJson`，也可以反向将 `definePageJson` 同步为 `<json>`，让页面配置修复流程更完整。
