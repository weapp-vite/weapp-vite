---
'create-weapp-vite': patch
'@wevu/api': patch
---

同步升级仓库内 `miniprogram-api-typings` 到 `5.1.2`，并更新 `create-weapp-vite` 模板 catalog 与 `@wevu/api` 的类型来源、微信 API 名单生成产物，确保脚手架生成结果和 API 类型基线与当前 workspace 保持一致。
