---
"@mpcore/simulator": patch
---

使用完整 CSS 选择器解析与查询引擎，修复属性子串、带空格属性值、组合选择器和重复匹配，并保留小程序组件别名查询。无效查询明确报错，避免误判为节点不存在。

修复循环展开与组件渲染后父节点仍指向原始模板的问题，让动态 ID 的祖先、直接子节点和相邻兄弟选择器按实际渲染树匹配。自动化原生交互事件补齐随宿主时钟变化的 timeStamp，同时保持未提供 detail 时的原生事件形态。
