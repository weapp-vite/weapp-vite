---
"create-weapp-vite": patch
---

脚手架默认使用随包版本、跳过可选 AI skills，创建模板不再依赖额外网络查询。显式启用兼容版本查询时尊重 registry、作用域源、代理和 CA 配置，核心依赖与 Tailwind 共用超时预算，查询失败保留完整随包组合。新增 registry 参数、skills 安装超时清理和新项目的 pnpm 依赖构建脚本审批配置，修复 pnpm 12 正常安装被未声明的构建脚本阻断，并补充国内镜像、正常安装和构建验证，以及镜像延迟与缓存恢复指引。
