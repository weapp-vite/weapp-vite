---
"weapp-vite": patch
"create-weapp-vite": patch
---

继续优化 `weapp-vite dev` 的开发态终端布局。默认热键提示现在会显示更接近交互式工具的 footer 结构，例如 `READY  waiting for actions...` 与 `press h to show help, press q to quit`，使开发态会话的状态感与操作提示更清晰。
