---
'@weapp-vite/vscode': minor
'create-weapp-vite': minor
---

VS Code 扩展现在会为 `weapp-vite Pages` 视图中的当前页面节点提供更直接的快捷动作入口。当前页节点会带上独立状态上下文，并以内联方式暴露复制 route、定位 `app.json` 以及配置漂移同步等常用操作，减少从树视图切换到命令面板或编辑器上下文的次数。
