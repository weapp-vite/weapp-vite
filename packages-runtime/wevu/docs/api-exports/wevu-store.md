# wevu/store 类型导出清单

- 子路径: `./store`
- 类型入口: `./dist/store.d.mts`
- 运行时入口: `./dist/store.mjs`
- 类型导出总数: **11**
- 仅类型导出数（推断）: **8**
- 运行时同名导出数: **3**

## 类型导出（完整）

1. `ActionContext`
2. `ActionSubscriber`
3. `DefineStoreOptions`
4. `MutationType`
5. `StoreManager`
6. `StoreSubscribeOptions`
7. `StoreToRefsResult`
8. `SubscriptionCallback`
9. `createStore`
10. `defineStore`
11. `storeToRefs`

## 仅类型导出（推断）

1. `ActionContext`
2. `ActionSubscriber`
3. `DefineStoreOptions`
4. `MutationType`
5. `StoreManager`
6. `StoreSubscribeOptions`
7. `StoreToRefsResult`
8. `SubscriptionCallback`

## 与运行时同名导出

1. `createStore`
2. `defineStore`
3. `storeToRefs`
