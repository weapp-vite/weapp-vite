import type { WeapiCrossPlatformAdapter } from '../../types'

export interface WeapiCrossPlatformMethodDocsMissingMethodsOffLocalServiceResolveFailToOffVoIPChatInterrupted {
  /**
   * 对应微信小程序 `wx.offLocalServiceResolveFail` 的 API。
   *
   * 分类：网络
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/network/mdns/wx.offLocalServiceResolveFail.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('offLocalServiceResolveFail')` 与 `wpi.supports('offLocalServiceResolveFail')` 判断。
   */
  offLocalServiceResolveFail: WeapiCrossPlatformAdapter['offLocalServiceResolveFail']

  /**
   * 对应微信小程序 `wx.offLocationChange` 的 API。
   *
   * 分类：位置
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/location/wx.offLocationChange.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('offLocationChange')` 与 `wpi.supports('offLocationChange')` 判断。
   */
  offLocationChange: WeapiCrossPlatformAdapter['offLocationChange']

  /**
   * 对应微信小程序 `wx.offLocationChangeError` 的 API。
   *
   * 分类：位置
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/location/wx.offLocationChangeError.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('offLocationChangeError')` 与 `wpi.supports('offLocationChangeError')` 判断。
   */
  offLocationChangeError: WeapiCrossPlatformAdapter['offLocationChangeError']

  /**
   * 对应微信小程序 `wx.offMenuButtonBoundingClientRectWeightChange` 的 API。
   *
   * 分类：界面
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/ui/menu/wx.offMenuButtonBoundingClientRectWeightChange.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('offMenuButtonBoundingClientRectWeightChange')` 与 `wpi.supports('offMenuButtonBoundingClientRectWeightChange')` 判断。
   */
  offMenuButtonBoundingClientRectWeightChange: WeapiCrossPlatformAdapter['offMenuButtonBoundingClientRectWeightChange']

  /**
   * 对应微信小程序 `wx.offNetworkStatusChange` 的 API。
   *
   * 分类：设备
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/device/network/wx.offNetworkStatusChange.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('offNetworkStatusChange')` 与 `wpi.supports('offNetworkStatusChange')` 判断。
   */
  offNetworkStatusChange: WeapiCrossPlatformAdapter['offNetworkStatusChange']

  /**
   * 对应微信小程序 `wx.offNetworkWeakChange` 的 API。
   *
   * 分类：设备
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/device/network/wx.offNetworkWeakChange.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('offNetworkWeakChange')` 与 `wpi.supports('offNetworkWeakChange')` 判断。
   */
  offNetworkWeakChange: WeapiCrossPlatformAdapter['offNetworkWeakChange']

  /**
   * 对应微信小程序 `wx.offPageNotFound` 的 API。
   *
   * 分类：基础
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/base/app/app-event/wx.offPageNotFound.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('offPageNotFound')` 与 `wpi.supports('offPageNotFound')` 判断。
   */
  offPageNotFound: WeapiCrossPlatformAdapter['offPageNotFound']

  /**
   * 对应微信小程序 `wx.offParallelStateChange` 的 API。
   *
   * 分类：界面
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/ui/window/wx.offParallelStateChange.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('offParallelStateChange')` 与 `wpi.supports('offParallelStateChange')` 判断。
   */
  offParallelStateChange: WeapiCrossPlatformAdapter['offParallelStateChange']

  /**
   * 对应微信小程序 `wx.offScreenRecordingStateChanged` 的 API。
   *
   * 分类：设备
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/device/screen/wx.offScreenRecordingStateChanged.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('offScreenRecordingStateChanged')` 与 `wpi.supports('offScreenRecordingStateChanged')` 判断。
   */
  offScreenRecordingStateChanged: WeapiCrossPlatformAdapter['offScreenRecordingStateChanged']

  /**
   * 对应微信小程序 `wx.offThemeChange` 的 API。
   *
   * 分类：基础
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/base/app/app-event/wx.offThemeChange.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('offThemeChange')` 与 `wpi.supports('offThemeChange')` 判断。
   */
  offThemeChange: WeapiCrossPlatformAdapter['offThemeChange']

  /**
   * 对应微信小程序 `wx.offUnhandledRejection` 的 API。
   *
   * 分类：基础
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/base/app/app-event/wx.offUnhandledRejection.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('offUnhandledRejection')` 与 `wpi.supports('offUnhandledRejection')` 判断。
   */
  offUnhandledRejection: WeapiCrossPlatformAdapter['offUnhandledRejection']

  /**
   * 对应微信小程序 `wx.offUserCaptureScreen` 的 API。
   *
   * 分类：设备
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/device/screen/wx.offUserCaptureScreen.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('offUserCaptureScreen')` 与 `wpi.supports('offUserCaptureScreen')` 判断。
   */
  offUserCaptureScreen: WeapiCrossPlatformAdapter['offUserCaptureScreen']

  /**
   * 对应微信小程序 `wx.offUserOffTranslation` 的 API。
   *
   * 分类：微信
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/offUserOffTranslation.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('offUserOffTranslation')` 与 `wpi.supports('offUserOffTranslation')` 判断。
   */
  offUserOffTranslation: WeapiCrossPlatformAdapter['offUserOffTranslation']

  /**
   * 对应微信小程序 `wx.offUserTriggerTranslation` 的 API。
   *
   * 分类：微信
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/offUserTriggerTranslation.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('offUserTriggerTranslation')` 与 `wpi.supports('offUserTriggerTranslation')` 判断。
   */
  offUserTriggerTranslation: WeapiCrossPlatformAdapter['offUserTriggerTranslation']

  /**
   * 对应微信小程序 `wx.offVoIPChatInterrupted` 的 API。
   *
   * 分类：媒体
   * 文档链接：https://developers.weixin.qq.com/miniprogram/dev/api/media/voip/wx.offVoIPChatInterrupted.html
   *
   * 说明：
   * - 这里保留微信命名与参数、返回类型。
   * - 运行时跨平台映射、降级或不支持情况，可结合 `wpi.resolveTarget('offVoIPChatInterrupted')` 与 `wpi.supports('offVoIPChatInterrupted')` 判断。
   */
  offVoIPChatInterrupted: WeapiCrossPlatformAdapter['offVoIPChatInterrupted']
}
