<view class="raw-banner feature-card">
  <view class="feature-card__main">style definition target</view>
  <view wx:for="{{items}}" wx:key="id" bindtap="handleTap" data-tone="{{ item.tone }}">{{ item.label }}</view>
</view>
