{
  "name": "e2e-app-script-setup-macros-js-with-defaults-invalid",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wv dev",
    "dev:open": "wv dev -o",
    "build": "node -e \"const fs=require('fs'); fs.mkdirSync('dist',{recursive:true}); fs.writeFileSync('dist/.skip-build','skip build: this fixture is intentionally invalid and is verified by negative e2e tests\\n'); console.log('skip build: this fixture is intentionally invalid and is verified by negative e2e tests')\"",
    "dev:ui": "wv dev --ui",
    "build:ui": "node -e \"const fs=require('fs'); fs.mkdirSync('dist',{recursive:true}); fs.writeFileSync('dist/.skip-build','skip build: this fixture is intentionally invalid and is verified by negative e2e tests\\n'); console.log('skip build: this fixture is intentionally invalid and is verified by negative e2e tests')\"",
    "postinstall": "wv prepare"
  },
  "devDependencies": {
    "@weapp-vite/dashboard": "workspace:*",
    "weapp-vite": "workspace:*",
    "wevu": "workspace:*"
  }
}
