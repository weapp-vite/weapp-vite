<view class="native-uses-vue">
  <native-card
    id="native-card-feature"
    title="{{title}}"
    subtitle="{{subtitle}}"
    badge="{{badge}}"
  >
    <text class="native-uses-vue__note">{{note}}</text>
  </native-card>
</view>
