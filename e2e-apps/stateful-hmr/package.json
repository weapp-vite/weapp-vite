{
  "name": "e2e-app-stateful-hmr",
  "version": "0.0.0",
  "private": true,
  "workspaceHmr": {
    "runtime": "stateful"
  },
  "scripts": {
    "build": "wv build",
    "build:web": "wv build -p web",
    "dev": "wv dev",
    "dev:web": "wv dev -p web --host",
    "dev:open": "wv dev -o",
    "postinstall": "wv prepare"
  },
  "devDependencies": {
    "weapp-vite": "workspace:*",
    "wevu": "workspace:*"
  }
}
