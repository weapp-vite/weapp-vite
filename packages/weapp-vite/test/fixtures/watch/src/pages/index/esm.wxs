const x = require('./cjs.wxs')
let xx = './dep.wxs'
let y = `./dep1.wxs`
let z = `./dep2` + '.wxs'
require(xx)
require(y)
require(z)
function tapName(event, ownerInstance) {
  console.log('tap 撒的撒打算', JSON.stringify(event))
}
