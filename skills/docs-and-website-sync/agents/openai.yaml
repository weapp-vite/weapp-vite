interface:
  display_name: Docs And Website Sync
  short_description: 根据真实实现同步 website、docs、skills、README 和脚手架 AI 指引
  default_prompt: >-
    当任务主轴是根据当前实现同步 website、docs、README、skills、AI 指南、dist/docs 或脚手架 AGENTS.md 时，
    使用 $docs-and-website-sync。先核对源码、类型、CLI 和生成脚本，再同步公开入口与触发回归。

policy:
  allow_implicit_invocation: true
