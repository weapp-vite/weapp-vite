interface:
  display_name: 'Release And Changeset Best Practices'
  short_description: 'pnpm change、repoctl 发布、Issue 与交付闭环'
  default_prompt: 'Use $release-and-changeset-best-practices to decide pnpm change intents, linked package bumps, repoctl release commands, verification, commit strategy, and issue-to-PR delivery for this repository.'

policy:
  allow_implicit_invocation: true
