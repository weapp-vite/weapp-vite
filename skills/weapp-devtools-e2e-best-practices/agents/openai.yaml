interface:
  display_name: 'Weapp DevTools E2E Best Practices'
  short_description: 'DevTools 与 Headless Runtime E2E、Parity 和验收'
  default_prompt: 'Use $weapp-devtools-e2e-best-practices to design serialized DevTools and headless runtime E2E coverage with residual-process cleanup, shared sessions, provider parity, stable assertions, and mpcore unit/browser/type regression layers.'

policy:
  allow_implicit_invocation: true
