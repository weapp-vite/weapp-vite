import { describe, it } from 'vitest'

describe.skip('unplugin-auto-import wevu presets DevTools runtime', () => {
  it('reuses one automator session across preset pages', async () => {
    // Provider-specific execution is wired by the repository IDE harness.
  })
})
